# MF 版本组合 → PS 兼容性映射
#
# 此文件是 compat_matrix.py 的对外投影——更新 compat_matrix.py 时同步更新此文件。
# 每个条目表示"此 MF 版本组合已通过 PS 精度红线验证"。
# 当 MF 升级框架版本时，新版本组合必须在此有对应条目才能启用 PS。
#
# PS 自身修 bug 发新 commit:
#   重新在目标 MF 版本组合上跑 NPU 精度验证 → 更新对应条目的 ps_commit + verified 日期。
#   MF 下次重新构建镜像（跑 Skill 4）时自动拉到新 commit。

entries:
  - key: "mf-deepseek-v4-flash-v25"
    # ps_commit = 2026-09-09 unit mock 对齐: g2_attention 测试补 MockContext.stats
    #   + 单进程 get_rank 替身(08-28+ 运行时漂移;ps_verify 78/78)。
    # ps_commit = 2026-09-09 fix13-rc: g2_attention 重算计数 None 守卫
    #   (ps_verify 78/78: test_provider_store_kv attention_module=None mock 形态修复)。
    # ps_commit = 2026-09-09 local golden sync: host-patches 四件替换为本地 patch-bundle
    #   09-07 终态(Z1-re/_cp_m-clamp/topk-clamp 下界/BSND pad zero-fill + verl-env
    #   device.py 还原 + verify-host-patches 锚点更新; 经 SC-1 run14 / SC-4 生产验证)。
    #   仅本地调和(G6 clean-bake 验证用), 未推远端 — push = automodelwire 侧后续动作。
    # 注: 历史基线 c2aaf74(08-31) 已由本 commit 取代(内容=本地 golden)。
    # ps_commit = 2026-09-02 pin 决策：c2aaf74(remote HEAD) —— 含 host-patches 三件装
    #   + 3 功能性修复(ccd98dd BSND-warning / ddd1e28 CP2-fix17b / 504a776 g2_attention)
    #   + M3/M4 修复。peer 真 PS-on 验证(2c + f1long20b 20 步全绿)停在 f513612(无 host-patches),
    #   c2aaf74 的运行时验证由 Skill 4.5 验收门禁承担(SC-2/SC-4 未跑即禁放行)。
    ps_commit: "b24bd8a5dfdff6cd80ac755b93fa31aee8261b9b"
    verl: "0.8.0.dev"
    megatron_core: "0.12.1"
    mindspeed: null               # dsv4 容器 mindspeed 无版本标记
    cann: "9.1.0-beta.3"
    patch_set_id: "verl080_mcore0121_dsv4"
    verified: "2026-09-09"
    notes: "18/18/30/30 precision tests used to be run on EARLIER code (07-22~08-03), NOT c2aaf74; c2aaf74 runtime validation = Skill 4.5 gate (SC-2/SC-4 pending)"
    test_results:
      unit_tests: "225/225 passing (Mac) [tested on pre-c2aaf74 code]"
      npu_tests: "30/30 passing (NPU) [tested on pre-c2aaf74 code]"
      precision: "18/18 bitwise equivalence [tested on pre-c2aaf74 code]"
